# Fonts

- `feijoa-medium.woff2` — **Test Feijoa Medium** (Klim Type Foundry). Body copy.
  Licensed by the site owner. Do not redistribute.

The wordmark ("Safe Flight") is **not** a webfont — it lives as an outlined vector at
`../assets/wordmark.svg` (originally Exposure Italic, outlined under a desktop license).
